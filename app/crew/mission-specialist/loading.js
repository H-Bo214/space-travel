export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  // Create a loading.js file in each directory to handle content loading delays and display information to the user.
  return (
    <div>
      <h1>Loading.....</h1>
    </div>
  )
}